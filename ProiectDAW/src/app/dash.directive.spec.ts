import { DashDirective } from './dash.directive';

describe('DashDirective', () => {
  it('should create an instance', () => {
    const directive = new DashDirective();
    expect(directive).toBeTruthy();
  });
});
